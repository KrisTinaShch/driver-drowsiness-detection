# Библиотеки проекта «Детектор усталости водителя».
#
# Версии зафиксированы под конкретную машину: macOS на процессоре Intel,
# Python 3.9. Если на новом компьютере Python новее (3.11+), можно смело
# убрать все "==..." и поставить свежие версии — эти пины нужны только
# потому, что под старую связку Python+macOS уже не выпускают готовые сборки.

# --- работа с таблицами и графики ---
numpy==1.26.4
pandas==2.2.2
matplotlib==3.9.2
scikit-learn==1.5.2

# --- обучение нейросети ---
torch==2.2.2
torchvision==0.17.2

# --- изображения и видео с камеры ---
# ставим только contrib-версию: обычный opencv-python кладёт тот же самый
# модуль cv2 и конфликтует с ней
opencv-contrib-python==4.10.0.84

# --- поиск лица и глаз на кадре ---
mediapipe==0.10.14

# --- загрузка датасета с Kaggle (работает без токена) ---
kagglehub==0.3.13

# --- звуковая тревога ---
sounddevice==0.5.1

# --- среда для ноутбуков ---
jupyterlab==4.2.5
